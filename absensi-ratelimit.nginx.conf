# Absensi — nginx edge rate limit for the login endpoint.
# This file must live in the http{} context, so it is installed to
# /etc/nginx/conf.d/ by the deploy script (NOT into sites-available/).
limit_req_zone $binary_remote_addr zone=absensi_login:10m rate=10r/m;